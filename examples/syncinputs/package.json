{
  "name": "syncinputs",
  "version": "1.0.0",
  "description": "redactio example",
  "main": "syncinputs.tsx",
  "scripts": {
    "build": "npx tsc && npx esbuild --bundle --outfile=bundle.js syncinputs.js && npx tsc --build --clean"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.13.12",
    "typescript": "^4.4.4"
  }
}
