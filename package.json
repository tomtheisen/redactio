{
  "name": "redactio",
  "version": "1.0.0",
  "description": "TSX components for mutable long-lived DOM elements",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx tsc && npx esbuild --bundle --outfile=bundle.js ./out/app.js && npx tsc --build --clean"
  },
  "author": "Tom Theisen",
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.13.9",
    "typescript": "^4.4.4"
  }
}
